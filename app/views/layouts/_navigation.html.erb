<nav class="navbar navbar-expand-xl py-3 navbar-light bg-light" id="navbar">
<%= link_to "FindLost", root_path, class:"navbar-brand text-warning" %>
  <% if current_user %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <li class="user-name nav-item">
          <%= fa_icon 'user' %> 
        </li>
        <li class="log-out nav-item">
          <%= link_to("Logout", destroy_user_session_path,
              class:"btn btn-xs btn-danger logout-button", method: :delete) %>
        </li>
      </ul>
    </div>
  <% end %>
  <% if not user_signed_in? %>
    <ul class="navbar-nav ml-auto">
      <li class="log-in nav-item">
        <%= link_to("Login", new_user_session_path,
            class:"btn btn-xs text-warning signin-button") %>
      </li>
      <li class="log-in nav-item text-muted">
        <%= link_to("Sign up", new_user_registration_path,
            class:"btn btn-xs text-warning signup-button") %>
      </li>
    </ul>
  <% end %> 
</nav>